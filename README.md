# Delhi-Metro-project
Delhi Metro Route Finder is a C++ DSA project that uses Dijkstra’s Algorithm to compute the shortest path between metro stations. Each station has a unique ID and connections with distances. The system takes user input and displays the optimal route and total distance efficiently.
